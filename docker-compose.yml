services:
  db:
    image: postgres:latest
    container_name: postgres_db
    restart: always
    environment:
      POSTGRES_PASSWORD: admin123
      POSTGRES_USER: admin
      POSTGRES_DB: poli
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql
volumes:
  pg_data: